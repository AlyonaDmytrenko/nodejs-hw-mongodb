description: Unauthorized - session not found or invalid token
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 401
        message:
          type: string
          example: Session not found
    example:
      status: 401
      message: Session not found
